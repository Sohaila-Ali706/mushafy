import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mushafy.app',
  appName: 'Mushafy',
  webDir: 'dist/mushafy/browser'
};

export default config;
